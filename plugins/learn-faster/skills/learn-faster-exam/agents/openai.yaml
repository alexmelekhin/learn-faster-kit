interface:
  display_name: "Learn FASTER Exam"
  short_description: "Generate printable exams, answer keys, and PDFs"
  default_prompt: "Use $learn-faster-exam to generate a printable mock exam."
policy:
  allow_implicit_invocation: false
